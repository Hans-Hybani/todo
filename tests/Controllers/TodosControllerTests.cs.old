// FILE KEPT FOR REFERENCE PURPOSES — NOT USED BY THE APPLICATION
// **************************************************************

// This test checked `TodosController.ToTitleCase`, which was removed during refactoring
// (see Controllers/TodosController.cs for the full explanation). This
// logic is now handled on the client side via the native Angular
// `titlecase` pipe, which no longer needs to be tested on the server side.
//
// See Controllers/TodoListControllerTests.cs for the new tests.


/*
using Shouldly;
using Xunit;

namespace Exercise.Todos.Controllers.Tests
{
    public class TodosControllerTests
    {
        [Fact]
        public void Should_TitleCase_WhenStringIsSingleCharacter()
        {
            var singleCharacterString = TodosController.ToTitleCase("h");

            singleCharacterString.ShouldBe("H");
        }
    }
}
*/